// Type Alias
// function makeCoordinate(point: { x: number; y: number }): {
//   x: number;
//   y: number;
// } {
//   return { x: Math.random(), y: Math.random() };
// }
function makeCoordinate(point) {
    return { x: Math.random(), y: Math.random() };
}
